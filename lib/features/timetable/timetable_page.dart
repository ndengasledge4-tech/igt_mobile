import 'package:flutter/material.dart';

import 'planning_page.dart';

class TimetablePage extends StatelessWidget {
  const TimetablePage({super.key});

  @override
  Widget build(BuildContext context) => const PlanningPage();
}
